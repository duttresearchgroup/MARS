#ifndef __core_sparta_initial_h
#define __core_sparta_initial_h

#include "base/base.h"

//creates the initial map for the tasks
//used in the offline simulator only
void vitamins_initial_map_task(model_sys_t *sys);

#endif
